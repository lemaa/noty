import React  from 'react';
import Container from 'react-bootstrap/Container';
import FormNote from './../components/form/index';

const Note = () => {
 
    return (
        <Container fluid>
            <FormNote></FormNote>
        </Container>          
    );
  }
  
export default Note;
